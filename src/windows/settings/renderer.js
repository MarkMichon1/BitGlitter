const axios = require('axios')
const { remote } = require('electron')

// Write output dir path
const ding = 3

// Read output dir path

// Enable bad frame strikes

// Bad frame strikes

// Maximum CPU cores

// Enable statistics

// Output stream name in write

// Logging enable

// Logging debug enable

// Logging output dir path


/*
40 char max for paths
 */



axios.get('http://localhost:7218/config/settings').then(() => {

})